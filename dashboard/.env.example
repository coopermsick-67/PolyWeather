# Local dev: leave unset. The Vite dev server proxies /api/dashboard to a
# local Python subprocess (see vite.config.js) and this can stay blank.
#
# Production build (Vercel, etc.): set this to your deployed backend's URL,
# e.g. https://polyweather-api.onrender.com
VITE_API_BASE_URL=
